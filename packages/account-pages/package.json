{
    "name": "account-pages",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "deploy": "wrangler pages deploy static --project-name account-pages",
        "dev": "wrangler pages dev static --port 48080",
        "start": "wrangler pages dev static --port 48080"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20240117.0",
        "typescript": "^5.0.4",
        "wrangler": "^3.0.0"
    }
}
