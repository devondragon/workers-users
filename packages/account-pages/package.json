{
    "name": "account-pages",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "deploy": "wrangler pages deploy static --project-name account-pages --branch=production",
        "dev": "wrangler pages dev static --port 48080 --ip '127.0.0.1' --inspector-port=9230",
        "start": "wrangler pages dev static --port 48080 --ip '127.0.0.1' --inspector-port=9230"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20240117.0",
        "typescript": "^5.0.4",
        "wrangler": "^3.0.0"
    }
}
